<nav class="navbar" role="navigation">
  <!-- We use the fluid option here to avoid overriding the fixed width of a normal container within the narrow content columns. -->
  <div class="container-fluid">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-6">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <label class="navbar-brand white">PPH</label>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-6">
        <ul class="nav navbar-nav">
            <li class="active"><%= link_to 'Página inicial', root_path, :class => 'home_links'%></li>
            <% if user_signed_in? %>
                <li> <%= link_to "Algoritmos", algorithms_index_path, :method => :get, :class => 'home_links'%></li>
            <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right white">
            <% if user_signed_in? %>
                <ul>
                    <%if !current_user.name.nil?%>
                        <li> <%=label_tag "Bem vindo! "+current_user.name%></li>
                    <%else%>
                        <li> <%= label_tag "Bem vindo! "+current_user.email%></li>
                    <%end%>
                    <li> <%= link_to 'Histórico', historics_path, :class=>'home_links'%></li>
                    <li><%= link_to 'Alterar minha conta', edit_user_registration_path, :class => 'home_links' %></li>
                    <li><%= link_to "Sair", destroy_user_session_path, :method => :delete, :class => 'home_links' %></li>
                </ul>
            <% else %>
                <li><%= link_to user_facebook_omniauth_authorize_path,:class => 'home_links' do %>
                    <%= image_tag('facebook.png', width: '20', height: '20', class: 'image-circle')%>
                <%end%></li>
                <li><%= link_to('Entrar', new_user_session_path, :class => 'home_links')%></li>
                <li><%= link_to('Cadastrar', new_user_registration_path, :class => 'home_links') %></li>
            <% end %>
        </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>
